-- ============================================
-- BOOKS TABLE (main entity)
-- ============================================

CREATE TABLE IF NOT EXISTS books (
    uuid TEXT NOT NULL PRIMARY KEY,
    id INTEGER NOT NULL,
    title TEXT NOT NULL,
    subtitle TEXT,
    language TEXT,
    publication_date TEXT,
    description TEXT,
    rating REAL,
    suffix TEXT,
    status_uuid TEXT,
    created_at TEXT,
    updated_at TEXT
);

CREATE INDEX IF NOT EXISTS idx_books_title ON books(title);
CREATE INDEX IF NOT EXISTS idx_books_rating ON books(rating);
CREATE INDEX IF NOT EXISTS idx_books_status ON books(status_uuid);

-- Queries for books
upsertBook:
INSERT OR REPLACE INTO books(uuid, id, title, subtitle, language, publication_date, description, rating, suffix, status_uuid, created_at, updated_at)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);

getAllBooks:
SELECT * FROM books ORDER BY title ASC;

getBookByUuid:
SELECT * FROM books WHERE uuid = ?;

deleteAllBooks:
DELETE FROM books;

deleteBook:
DELETE FROM books WHERE uuid = ?;

getBooksCount:
SELECT COUNT(*) FROM books;

getBooksByRating:
SELECT * FROM books WHERE rating >= ? ORDER BY rating DESC;

searchBooksByTitle:
SELECT * FROM books WHERE title LIKE ? ORDER BY title ASC;

