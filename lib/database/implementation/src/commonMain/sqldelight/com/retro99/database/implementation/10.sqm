-- Migration from version 10 to version 11
-- Adds sort_order column to bookmarks for manual reordering
-- Renamed bookmarks use existing locator_title column (no new column needed)

ALTER TABLE bookmarks ADD COLUMN sort_order INTEGER NOT NULL DEFAULT 0;
